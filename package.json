{
  "name": "feedoff-rules",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "node build.mjs && node scripts/check-selectors.mjs"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
